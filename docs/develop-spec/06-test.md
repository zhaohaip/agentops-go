执行测试时必须遵守：

- 禁止并行启动多个全仓测试；
- 优先运行当前修改包的测试；
- Go 测试使用 `-p 2` 限制包并行度；
- 测试必须设置超时；
- 禁止启动无法自动退出的后台服务；
- 使用 `exec.Command().Start()` 时必须负责终止并调用 `Wait()`；
- 任务结束前检查并清理本次启动的子进程；
- 不得将 `go test`、服务进程或监听进程放到后台运行。